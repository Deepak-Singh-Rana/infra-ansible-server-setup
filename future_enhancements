1)allow generate_hosts.py to also generate extra roles to load? e.g. for deploying apache
2)get the py to generate a extraplays.yml that the newservers.yml mainplaybook will run?
3)just get people to fill a form and export it as csv, using activate
