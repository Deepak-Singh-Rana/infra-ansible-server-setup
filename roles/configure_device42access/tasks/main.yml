---
# tasks file for configure_upguardaccess

# configure device42
- include_tasks: configure_snmpd.yml

- include_tasks: redhat.yml
  when: ansible_facts['distribution'] == "Redhat"

- include_tasks: ubuntu.yml
  when: ansible_facts['distribution'] == "Ubuntu"
